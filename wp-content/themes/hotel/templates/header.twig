<header class="header">
  <div class="logo">
  </div>
  <div id="menu">
    <ul>
      {% for item in menu.items %}
        <a href="{{ item.link }}">
          <li>{{ item.title }}</li>
        </a>
      {% endfor %}
    </ul>
  </div>
</header>
